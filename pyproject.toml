[project]
name = "oberoon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.6",
    "requests-wsgi-adapter>=0.4.1",
    "webob>=1.8.9",
    "whitenoise>=6.12.0",
]

[dependency-groups]
dev = [
    "pytest>=8.3.2",
    "pytest-cov>=5.0.0",
]
